# JARVIS - Just A Rather Very Intelligent System
# Voice-activated AI assistant with local STT/TTS and Google Gemini brain.

# Voice
sounddevice>=0.4.6
soundfile>=0.12.1
numpy>=1.24.0
faster-whisper>=0.10.0
edge-tts>=6.1.0
# Resampling clips whose rate differs from Whisper's (voice_engine)
scipy>=1.18.1

# AI Brain - Google Gemini (brain_gemini imports the google-genai SDK)
google-genai>=2.10.0
# The router, Groq and Cerebras backends speak the OpenAI API; the router
# leads every turn, so this is required even though it is imported lazily.
openai>=2.53.0

# Browser automation (ask_chatgpt)
playwright>=1.40.0
# Clicking inside the user's own browser over its debugging port (browser_cdp)
websockets>=13.0

# Web HUD server (jarvis_web.py)
fastapi>=0.139.0
uvicorn>=0.51.0

# Desktop app steps and deep window scans (desktop_driver, app_abilities)
pywinauto>=0.6.9; sys_platform == "win32"

# Utilities
python-dotenv>=1.0.0
rich>=13.0.0

# Optional features, imported lazily and skipped when missing. Install only
# what you use:
#   kokoro-onnx            JARVIS_TTS_ENGINE=kokoro (models/kokoro)
#   piper-tts              JARVIS_TTS_ENGINE=piper
#   sentence-transformers  semantic memory search (pulls in PyTorch)
#   model2vec              semantic router (numpy only; downloads
#                          minishlab/potion-multilingual-128M on first use)
#   pycaw                  lowering other apps' volume while JARVIS speaks
#   python-docx            Word reports (report_agent)
#   Pillow                 window screenshots (desktop_driver)
#   pyautogui              one keyboard fallback in tools
# turbovec and honcho are local experiments, not needed to run JARVIS.