# Python
__pycache__/
*.pyc
*.pyo

# Secrets / local env (NEVER commit)
.env
.env.local
*.env
.env.bak.*
*.env.bak*

# Generated report artifacts
Documents/
*.docx

# Logs
*.log
chrome_dbg.log

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/

# Backup artifacts
*.bak
# Pre-v3 HUD rollback snapshot (kept on disk, not in repo)
jarvis_hud_v3.backup.html
# Local Gemini task briefs (transient, not repo artifacts)
.gemini-brief-hud.md

# Web app registry — auto-generated, contains visit history
web_registry.json

# Local-only persistent memory (PRIVACY: never commit)
# Tier-2 durable profile (user/machine facts, harness contract)
jarvis-profile.md
# Tier-3 JARVIS session transcripts (everything said to JARVIS)
sessions/
# Capability manifest (leaks installed software inventory)
capabilities.json
# Local-only Manus cookie handoff (PRIVACY: never commit — like .env)
manus_cookies.json
# Any *-verify temp probes (must never be committed)
hermes-verify-*.py
app_registry.json
# Audit trail + job logs (privacy, grows fast)
logs/
# Warm harness cache + autonomous status files
.warm_harness/
.hermes-jobs/

# Recovery + large local artifacts (never commit binaries or private memory)
.recovery/
models/
memory/
nul
